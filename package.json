{
  "private": true,
  "name": "betBot",
  "version": "1.1.0-alpha.0",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "typings": "index.d.ts",
  "homepage": "https://github.com/Techmob-Show/automation",
  "files": [
    "src",
    "dist/*.js",
    "*.d.ts"
  ],
  "scripts": {
    "start": "node dist/index.js",
    "build": "npm run compile",
    "dev": "npm run build && npm run start",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json",
    "lint": "tslint --fix --project ./tsconfig.json"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "playwright": "^1.11.1"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}
